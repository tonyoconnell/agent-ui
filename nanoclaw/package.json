{
  "name": "nanoclaw",
  "version": "1.0.0",
  "private": true,
  "description": "Edge-native agents on Cloudflare free tier",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 execute one --file=migrations/0001_init.sql",
    "migrate:local": "wrangler d1 execute one --local --file=migrations/0001_init.sql",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^2.0.41",
    "ai": "^6.0.164",
    "hono": "^4.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.0",
    "wrangler": "^4.80.0"
  }
}
