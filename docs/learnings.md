- 2026-04-20 · security cycle 1 · wave 4|gate · WIRE-DELETE: security.ts deleted (XOR is not encryption); sanitizeUrl+maskSensitive already in utils.ts; ContractsPage switched to sessionStorage; harden.test.ts now fails (3 tests) because Cycle 0 added auth and mocks don't supply Authorization — fix in Cycle 3 test pass · rubric=0.92 · source=w4
- 2026-04-20 · security cycle 0 · wave 4|gate · STOP-BLEED: AdminCap gates Move treasury + fee setter; Unit.owner + caller assertion closes mark/warn on routing graph; validateApiKey gate on query/decay/resistance/harden; gateway /typedb/query behind GATEWAY_API_KEY; SSRF: Authorization stripped + host allowlist in sseProxy; scope-group/scope-skill schema attrs activate dormant scope check; dissolve() struct destructure must be exhaustive (V1 catch) · rubric=0.872 · source=w4
- 2026-04-20 · cycle 2 · wave 4|gate · subscribe routing: tag:X receivers resolve via D1 subscriptions table before BAAS metering; let-alias pattern for const destructure reassignment; lazy import('@/lib/subscribe-routing') keeps cold-start clean · rubric=0.875 · source=w3 w4
- 2026-04-20 · groups cycle 1 · wave 4|gate · W3 scope must start with grep not assumed file list — 14 extra group-id references found across 6 files outside TODO scope; fade-rate owned by group entity but undeclared in one.tql (stale comment pointed to world.tql) · rubric=0.82 · source=w4
- 2026-04-20 · groups cycle 2 · wave 4|gate · getRoleForUser(uid) without gid checks global role not group membership — every read gate on a private resource must pass gid; members.ts had auth but no membership check (private roster exposed to any authed user); git stash resets .audit-baseline.json (reseed after any stash restore) · rubric=0.88 · source=w4
- 2026-04-20 · groups cycle 3 · wave 4|gate · bridge.ts chairman check is role-agnostic (any member can initiate handshake, not just chairman) — more permissive than the doc contract but not a security hole; suggestedJoins TQL missing "not already member" predicate (shows all public groups, safe but imprecise); TypeDB 3.x fun is non-recursive so hierarchy closure is single-hop children_of; git stash drop after failed pop deletes ALL tracked working-tree changes — never stash drop without verifying pop succeeded · rubric=0.84 · source=w4
- 2026-04-20 · chairman cycle 1 · gate · WsHub unit-hired broadcast wired end-to-end: engine → gateway allowlist → stream hook → OrgChart pending nodes; DIRECTOR_UIDS+polling removed · rubric=0.85 · source=cycle
- 2026-04-20 · chairman cycle 2 · gate · Extracted PheromoneEdge+useNodeLayout+useOrgPaths; OrgChart edges now live on /api/export/paths; WorldGraph scope-deferred (incompatible layout model) · rubric=0.83 · source=cycle
- 2026-04-20 · chairman cycle 3 · gate · RoleCatalog surfaces agents/roles/*.md dynamically; /api/chairman/roles new; build-team accepts {roles[]}; engine handler reads data.roles with fallback · rubric=0.85 · source=cycle
- 2026-04-20 · chairman plan · close · All 3 cycles shipped: C1 live WsHub stream, C2 pheromone edge extraction, C3 role catalog. Chairman page now event-native, configurable, and pheromone-aware. · rubric=0.84 · source=plan
- 2026-04-20 · security cycle 7 · wave 4|gate · SUI-LOCK: (amount / 10000) * fee_bps not (amount * fee_bps) / 10000 — all 3 payment functions fixed; Path.hardened bool + assert(!path.hardened) closes double-harden; settleEscrow returns Promise<{ok,error?}> with TypeDB failure write; EInvalidFee+EInvalidRate assert bounds before state change · rubric=0.921 · source=w4
- 2026-04-20 · security cycle 5 · wave 4|gate · SHIELD-DATA: tenant-kek entity added to world.tql (C-4 — encryptForGroup was silently falling back to plaintext); shredGroup wired into forget GDPR cascade; deriveKeypair version-0 preserved for on-chain wallets, version-1 HKDF added for new agents; TypeDB 3.x uses `entity <name>` (not `<name> entity`) and `value integer` (not `value long`) · rubric=0.67 · source=w4
- 2026-04-20 · security cycle 4 · wave 4|gate · LEARN-SIGNALS: 11 security failure paths now emit+warn; PEP-3 capability-missing was missing net.warn (pheromone gap); PEP-4 schema enforce was silent; Sui session + BetterAuth paths instrumented; revoke emits to WsHub via gateway (added 'revoke' to ALLOWED_MESSAGE_TYPES); every vi.mock(@/lib/api-key) needs getKeyPrefix when api-auth.ts is in scope · rubric=0.84 · source=w4
- 2026-04-20 · security cycle 3 · wave 4|gate · LOCK-PEP: escapeTqlString() in typedb.ts closes TQL injection in resistance/harden/discover; nonce moved from PEP-2 to PEP-5a in ask() so replay tokens only consumed if delivery proceeds; harden.test.ts needed escapeTqlString pass-through + api-auth mock (user must match uid in body) · rubric=0.79 · source=w4
- 2026-04-20 · security cycle 2.5 · wave 4|gate · ENGINE-HARDEN: NONCE_SEEN+LIFECYCLE_CACHE moved from module scope into world() factory closure (C-2: concurrent worlds were sharing one nonce set — replay protection was per-process not per-world); wave-runner.ts markDims import fixed to rubric-score.ts 0.65 gate (was rubric.ts 0.5 — pheromone was treating mediocre as good); bridge.ts fail-open comment removed + allowedHosts.length=0 explicit; agentverse-connect.ts process.env → import.meta.env (CF Workers fix); typedb.ts fallback URL api.one.ie + GATEWAY_API_KEY Authorization header (Cycle 0 had gated the gateway but typedb.ts never sent the key — every TypeDB call was 401 in prod) · rubric=0.95 · source=w4
- 2026-04-20 · security cycle 2 · wave 4|gate · SCOPE-KEYS: key-prefix attribute in world.tql enables O(1) TypeDB prefix-filtered PBKDF2 lookup (fixes H-12 O(n) scan); getKeyPrefix strips non-alphanumeric chars to prevent TQL injection via crafted bearer tokens (W4 catch); Math.random keyId → crypto.randomUUID() in agent.ts + api-keys.ts (fixes L-16); permissions default "read" not "read,write" (least privilege); 24h expires-at TTL on all new keys; api-keys.ts now accepts scopeGroup/scopeSkill body params · rubric=0.90 · source=w4
- 2026-04-20 · u-todo cycle 4 · wave 4|gate · U-HARDEN: 24 integration test files (vault invariants × 10, chain × 6, migration, signer-iface, sign, pay, zklogin-isolation, pages-smoke, sui-core, send-pay, paylink); Signer abstraction layer (4 adapters, useSigner hook); lifecycle signals in useVault.ts + useWallets.ts; fake-indexeddb installed for IndexedDB tests in Node; isolation contract grep uses import-only regex not string presence; SUPPORTED_NETWORKS is Record not array → Object.values(); Function.length counts TS optional params (length=2 not 1); phase2-keypair 401 failures are pre-existing (live gateway required) · rubric=0.89 · source=w4
- 2026-04-20 · auth-todo cycle 1 · wave 4|gate · AUTH-CLAIM: human-agent ownership handshake complete; tasks 1-5 already implemented (remint gate + claim endpoint + bearer role + ownership group + deriveHumanUid); task 6 (integration tests) created: 14 tests across claim endpoint, remint gate, deriveHumanUid; readFileSync relative path must be ../../ not ../../../../ from __tests__/integration/; agent.ts has unmockable CF D1 + Sui deps — tests g/h/i converted to structural source assertions; getKeyPrefix must be in @/lib/api-key mock when api-auth chain is in scope · rubric=0.86 · source=w4
- 2026-04-20 · marketplace-experience-todo cycle 4 · wave 4|gate · REFINE: ReceiptPanel imported visibleStage() fixing 10→3 stage collapse drift; useMarketplaceSocket hook adds WS mark/warn filter by uid; successRate from TQL aggregation surfaced in OfferPanel; 6 a11y gaps closed (aria-valuenow/max, aria-hidden decorative, aria-label buttons); biome clean, tsc clean, 1780/1807 pass (4 pre-existing gateway 401s) · rubric=0.875 · source=w4
- 2026-04-29 · cycle 1 · wave gate · auth-ui lean cycle: 5-door auth surface (passkey/google/wallet/email/password-opt-in) shipped with no-enumeration + seed-bind-per-door + CF Workers env fallback + composite rate limiting; tsc has pre-existing stack-overflow crash (not our regression); loop.test flaky under full suite but passes in isolation · rubric=0.85 · source=w4
