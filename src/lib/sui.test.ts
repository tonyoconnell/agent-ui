// sui.test.ts — identity tests removed in sys-201 (platform key derivation deleted)
// Remaining Sui tests live in src/__tests__/integration/ and src/__tests__/bench/
import { describe, it } from 'vitest'

describe('sui', () => {
  it.todo('capability-based mark/warn tests — sys-201 follow-up')
})
