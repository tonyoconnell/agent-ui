---
export const prerender = true;

/**
 * /u — Wallet home (wallet-v2: IndexedDB seed, passkey-PRF, ephemeral-first)
 * Replaced UDashboard with SignPage; UDashboard still lives at /u/dashboard
 */
import Layout from "@/layouts/Layout.astro";
import { SignPage } from "@/components/u/pages/SignPage";
---

<Layout title="Wallet — ONE">
  <SignPage client:only="react" />
</Layout>
