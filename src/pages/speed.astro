---
import Layout from "@/layouts/Layout.astro"
import { SpeedJourney } from "@/components/speed/SpeedJourney"
import { LifecycleSpeedrun } from "@/components/speed/LifecycleSpeedrun"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import {
  Card,
  CardContent,
  CardDescription,
  CardHeader,
  CardTitle,
} from "@/components/ui/card"
import {
  Activity,
  Cpu,
  GitBranch,
  Layers,
  Repeat,
  Shield,
  Sparkles,
  Zap,
} from "lucide-react"

export const prerender = true

// Verified benchmarks surfaced from docs/speed-test.md — PERF_SCALE=3, 125 samples.
// p50 on idle hardware. Budget is the hard target; p50 is observed.
type Bench = { op: string; budget: string; p50: string; replaces: string }

const routingBench: Bench[] = [
  { op: "select() from 100 paths",   budget: "0.005ms", p50: "0.007ms", replaces: "LLM routing (~300ms)" },
  { op: "select() from 1,000 paths", budget: "1.00ms",  p50: "0.074ms", replaces: "search API + rank" },
  { op: "follow strongest",          budget: "0.050ms", p50: "0.005ms", replaces: "keyword search" },
  { op: "follow batch (10k)",        budget: "0.005ms", p50: "0.005ms", replaces: "—" },
]

const pheromoneBench: Bench[] = [
  { op: "mark()",            budget: "0.001ms", p50: "0.45μs",  replaces: "DB write (~10ms)" },
  { op: "warn()",            budget: "0.001ms", p50: "0.22μs",  replaces: "—" },
  { op: "sense()",           budget: "0.001ms", p50: "0.04μs",  replaces: "—" },
  { op: "fade() over 1,000", budget: "5.00ms",  p50: "0.137ms", replaces: "—" },
  { op: "highways() top-10", budget: "5.00ms",  p50: "0.92μs",  replaces: "—" },
]

const signalBench: Bench[] = [
  { op: "dispatch (in-memory)", budget: "1.00ms", p50: "0.72μs",  replaces: "HTTP call (~50ms)" },
  { op: "dispatch: dissolved",  budget: "1.00ms", p50: "0.12μs",  replaces: "—" },
  { op: "queue round-trip",     budget: "1.00ms", p50: "2.0μs",   replaces: "—" },
  { op: "ask: chain-3",         budget: "100ms",  p50: "0.017ms", replaces: "3 sequential LLM (~6s)" },
]

const infraBench: Bench[] = [
  { op: "edge cache hit",       budget: "0.010ms", p50: "0.52μs", replaces: "TypeDB RTT (~100ms)" },
  { op: "Sui keypair derive",   budget: "5.00ms",  p50: "2.45ms", replaces: "HSM round-trip" },
  { op: "Sui tx build",         budget: "0.010ms", p50: "0.51μs", replaces: "—" },
  { op: "Sui sign",             budget: "5.00ms",  p50: "0.36ms", replaces: "—" },
]

const benchCards = [
  { title: "Routing Layer",   ops: routingBench,   titleCls: "text-primary-bright",    p50Cls: "text-primary-bright" },
  { title: "Pheromone Layer", ops: pheromoneBench, titleCls: "text-tertiary-bright", p50Cls: "text-tertiary-bright" },
  { title: "Signal Layer",    ops: signalBench,    titleCls: "text-secondary-bright",  p50Cls: "text-secondary-bright" },
  { title: "Edge · Sui",      ops: infraBench,     titleCls: "text-gold",   p50Cls: "text-gold" },
] as const

const principles = [
  {
    icon: Cpu,
    title: "Routing is arithmetic",
    body: "weight = 1 + max(0, strength − resistance) × sensitivity. No ML. No coordinator. Just a single formula applied across proven paths.",
    stat: "<0.005ms / decision",
  },
  {
    icon: Layers,
    title: "The deterministic sandwich",
    body: "Toxicity → capability → LLM → mark/warn. Four of five layers cost $0. Only one is probabilistic. The rest is math wrapped around the model.",
    stat: "4 of 5 layers free",
  },
  {
    icon: Repeat,
    title: "Every signal closes its loop",
    body: "result → mark. failure → warn. dissolved → warn(0.5). Pheromone accumulates on paths automatically, so the system learns as it runs.",
    stat: "43,200 signals/day",
  },
]
---

<Layout
  title="Speed — Ride a Signal Through the World"
  description="Watch one real signal travel through 9 stops — click, edge, route, sandwich, LLM, mark, loops, highway, harden — with live timings at every step."
  imageAlt="ONE Speed — the 9-stop journey of a signal through the substrate"
>
  <main class="min-h-screen bg-background text-foreground">

    <!-- Hero -->
    <section class="relative overflow-hidden border-b border-border px-6 py-20 md:px-10 md:py-28">
      <div class="absolute inset-0 -z-10 bg-gradient-to-br from-primary/10 via-background to-transparent"></div>
      <div class="absolute -right-40 top-16 hidden h-[540px] w-[540px] rounded-full bg-primary/10 blur-3xl md:block"></div>
      <div class="absolute -left-40 bottom-0 hidden h-[440px] w-[440px] rounded-full bg-secondary/10 blur-3xl md:block"></div>

      <div class="mx-auto max-w-5xl space-y-8 text-center">
        <Badge variant="outline" className="border-primary/60 bg-primary/20 text-primary-bright font-mono text-xs uppercase tracking-wider">
          <Sparkles className="mr-1.5 h-3 w-3" /> live · verified · measured
        </Badge>

        <h1 class="text-4xl font-semibold leading-[1.05] tracking-tight text-font sm:text-5xl md:text-6xl lg:text-7xl">
          Ride a Signal
        </h1>

        <p class="mx-auto max-w-3xl text-base leading-relaxed text-muted-foreground md:text-lg">
          One real signal. Nine stops. Click → edge → route → sandwich → LLM →
          mark → loops → highway → harden. Every step measured, every step honest.
        </p>

        <div class="mx-auto flex max-w-3xl flex-wrap items-center justify-center gap-3 pt-4">
          <span class="inline-flex items-center gap-2 rounded-full border border-tertiary-bright/50 bg-tertiary-bright/20 px-4 py-1.5 font-mono text-xs text-tertiary-bright">
            <Zap className="h-3 w-3" /> routing &lt;0.005ms
          </span>
          <span class="inline-flex items-center gap-2 rounded-full border border-primary-bright/50 bg-primary-bright/20 px-4 py-1.5 font-mono text-xs text-primary-bright">
            <Activity className="h-3 w-3" /> mark &lt;0.001ms
          </span>
          <span class="inline-flex items-center gap-2 rounded-full border border-secondary-bright/50 bg-secondary-bright/20 px-4 py-1.5 font-mono text-xs text-secondary-bright">
            <GitBranch className="h-3 w-3" /> gateway &lt;10ms
          </span>
          <span class="inline-flex items-center gap-2 rounded-full border border-gold/50 bg-gold/20 px-4 py-1.5 font-mono text-xs text-gold">
            <Shield className="h-3 w-3" /> 320 tests · &lt;7s
          </span>
        </div>

        <div class="pt-4 text-xs font-mono text-muted-foreground">
          ↓ scroll — or skip to the <a href="#lifecycle" class="text-primary-bright hover:underline">lifecycle speedrun</a>
        </div>
      </div>
    </section>

    <!-- Section 1 · The Interactive Journey -->
    <section class="border-b border-border px-6 py-16 md:px-10 md:py-24">
      <div class="mx-auto max-w-5xl space-y-10">
        <div class="space-y-2">
          <p class="font-mono text-xs uppercase tracking-wider text-primary-bright">§ 01 · The Journey</p>
          <h2 class="text-3xl font-semibold tracking-tight text-font sm:text-4xl">
            Send a signal. Watch it travel.
          </h2>
          <p class="max-w-2xl text-sm text-muted-foreground md:text-base">
            Click the button. The signal fires into a real Cloudflare Worker,
            runs through every layer of the substrate, and streams its timing
            back over SSE. No mock. No delay. Live numbers.
          </p>
        </div>
        <SpeedJourney client:load />
      </div>
    </section>

    <!-- Section 2 · Why it's fast -->
    <section class="border-b border-border bg-muted/20 px-6 py-16 md:px-10 md:py-24">
      <div class="mx-auto max-w-6xl space-y-12">
        <div class="space-y-3 text-center">
          <p class="font-mono text-xs uppercase tracking-wider text-primary-bright">§ 02 · The Math</p>
          <h2 class="text-3xl font-semibold tracking-tight text-font sm:text-4xl">
            Why it&rsquo;s fast
          </h2>
          <p class="mx-auto max-w-2xl text-sm text-muted-foreground md:text-base">
            We don&rsquo;t compete on LLMs. Everyone&rsquo;s inference runs at physics speed.
            The difference is everything around it — and everything around it is arithmetic.
          </p>
        </div>

        <div class="grid gap-4 md:grid-cols-3">
          {principles.map((p) => (
            <Card className="bg-card border-border hover:border-foreground/20 transition-colors">
              <CardHeader className="space-y-3">
                <div class="flex h-10 w-10 items-center justify-center rounded-lg bg-primary/10 text-primary-bright">
                  <p.icon className="h-5 w-5" />
                </div>
                <CardTitle className="text-font">{p.title}</CardTitle>
                <CardDescription className="text-muted-foreground leading-relaxed">
                  {p.body}
                </CardDescription>
              </CardHeader>
              <CardContent>
                <div class="inline-flex items-center gap-2 rounded-md bg-muted px-3 py-1.5 font-mono text-xs text-tertiary-bright ring-1 ring-tertiary/40">
                  {p.stat}
                </div>
              </CardContent>
            </Card>
          ))}
        </div>
      </div>
    </section>

    <!-- Section 3 · Lifecycle speedrun -->
    <section id="lifecycle" class="border-b border-border px-6 py-16 md:px-10 md:py-24">
      <div class="mx-auto max-w-5xl space-y-10">
        <div class="space-y-2">
          <p class="font-mono text-xs uppercase tracking-wider text-primary-bright">§ 03 · End-to-End</p>
          <h2 class="text-3xl font-semibold tracking-tight text-font sm:text-4xl">
            Lifecycle speedrun
          </h2>
          <p class="max-w-2xl text-sm text-muted-foreground md:text-base">
            Full commerce loop on Sui testnet: create wallet, fund, list service,
            discover, message, sell, subscribe, browse, buy, verify. Ten stages.
            One continuous run. Real wallets, real digests.
          </p>
        </div>
        <LifecycleSpeedrun client:visible />
      </div>
    </section>

    <!-- Section 4 · Verified benchmarks -->
    <section class="border-b border-border bg-muted/20 px-6 py-16 md:px-10 md:py-24">
      <div class="mx-auto max-w-6xl space-y-12">
        <div class="space-y-3 text-center">
          <p class="font-mono text-xs uppercase tracking-wider text-primary-bright">§ 04 · Receipts</p>
          <h2 class="text-3xl font-semibold tracking-tight text-font sm:text-4xl">
            Verified benchmarks
          </h2>
          <p class="mx-auto max-w-2xl text-sm text-muted-foreground md:text-base">
            Every number below is produced by <code class="rounded bg-muted px-1.5 py-0.5 text-xs text-primary-bright">scripts/speed-report.ts</code>
            after every <code class="rounded bg-muted px-1.5 py-0.5 text-xs text-primary-bright">bun test</code>.
            p50 observed vs. budget target · PERF_SCALE=3 · 125 samples.
          </p>
        </div>

        <div class="grid gap-6 lg:grid-cols-2">
          {benchCards.map((group) => (
            <Card className="bg-card border-border">
              <CardHeader className="pb-3">
                <CardTitle className={`text-base text-primary-bright`}>{group.title}</CardTitle>
                <CardDescription className="text-xs text-muted-foreground">
                  budget · p50 observed · replaces
                </CardDescription>
              </CardHeader>
              <CardContent className="space-y-1.5">
                {group.ops.map((o) => (
                  <div class="grid grid-cols-[1fr_auto_auto] items-center gap-3 rounded-md border border-border/60 bg-muted/40 px-3 py-2">
                    <div class="min-w-0">
                      <p class="truncate text-sm font-mono text-foreground">{o.op}</p>
                      <p class="truncate text-[10px] text-muted-foreground">{o.replaces}</p>
                    </div>
                    <span class="font-mono text-[10px] uppercase tracking-wider text-muted-foreground">
                      budget <span class="text-foreground">{o.budget}</span>
                    </span>
                    <span class={`font-mono text-xs tabular-nums text-primary-bright`}>
                      {o.p50}
                    </span>
                  </div>
                ))}
              </CardContent>
            </Card>
          ))}
        </div>

        <p class="text-center text-xs text-muted-foreground">
          Full table: <a href="/speedtest" class="text-primary-bright hover:underline">/speedtest</a> ·
          source: <code class="text-muted-foreground">docs/speed-test.md</code>
        </p>
      </div>
    </section>

    <!-- CTA -->
    <section class="px-6 py-20 md:px-10 md:py-28">
      <div class="mx-auto max-w-3xl space-y-8 text-center">
        <h2 class="text-3xl font-semibold tracking-tight text-font sm:text-4xl">
          Run it yourself.
        </h2>
        <p class="text-sm text-muted-foreground md:text-base">
          Don&rsquo;t take our word for it. Every benchmark above is reproducible in
          under seven seconds on your machine. The substrate has no secret sauce —
          just 670 lines of engine and a formula.
        </p>
        <div class="flex flex-wrap items-center justify-center gap-3">
          <a href="/speedtest">
            <Button className="bg-primary-bright hover:bg-primary-bright/80 text-black font-medium">
              See the engine benchmarks
            </Button>
          </a>
          <a href="https://api.one.ie/health" target="_blank" rel="noopener noreferrer">
            <Button variant="outline" className="border-border text-foreground hover:bg-muted">
              Ping the gateway
            </Button>
          </a>
          <a href="https://dev.one.ie/api/export/highways" target="_blank" rel="noopener noreferrer">
            <Button variant="outline" className="border-border text-foreground hover:bg-muted">
              Export live highways
            </Button>
          </a>
        </div>
        <div class="pt-6 text-xs text-muted-foreground">
          <span class="font-mono">math + pheromone + 4-layer sandwich = </span>
          <span class="font-mono text-primary-bright">instant routing, forever learning.</span>
        </div>
      </div>
    </section>
  </main>
</Layout>
