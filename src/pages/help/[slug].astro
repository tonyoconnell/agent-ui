---
import Layout from "@/layouts/Layout.astro"

const { slug } = Astro.params
---

<Layout
  title={`Help — ${slug ?? "Article"} — ONE`}
  description="ONE help article. Get answers about your wallet, payments, and agents."
>
  <div class="min-h-screen bg-background text-foreground">

    <!-- Header -->
    <div class="border-b border-border bg-card px-6 py-6">
      <div class="mx-auto max-w-3xl flex items-center justify-between">
        <a
          href="/help"
          class="flex items-center gap-2 text-sm text-muted-foreground transition-colors hover:text-foreground"
        >
          <svg
            class="h-4 w-4"
            fill="none"
            stroke="currentColor"
            stroke-width="1.5"
            viewBox="0 0 24 24"
            aria-hidden="true"
          >
            <path stroke-linecap="round" stroke-linejoin="round" d="M15.75 19.5L8.25 12l7.5-7.5" />
          </svg>
          Back to help
        </a>
      </div>
    </div>

    <!-- Coming soon placeholder -->
    <div class="mx-auto flex max-w-3xl flex-col items-center justify-center px-6 py-24 text-center">
      <div class="mb-6 flex h-14 w-14 items-center justify-center rounded-full border border-border bg-card">
        <svg
          class="h-6 w-6 text-muted-foreground"
          fill="none"
          stroke="currentColor"
          stroke-width="1.5"
          viewBox="0 0 24 24"
          aria-hidden="true"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            d="M12 6.042A8.967 8.967 0 006 3.75c-1.052 0-2.062.18-3 .512v14.25A8.987 8.987 0 016 18c2.305 0 4.408.867 6 2.292m0-14.25a8.966 8.966 0 016-2.292c1.052 0 2.062.18 3 .512v14.25A8.987 8.987 0 0018 18a8.967 8.967 0 00-6 2.292m0-14.25v14.25"
          />
        </svg>
      </div>

      <h1 class="text-xl font-semibold text-foreground">Article coming soon</h1>
      <p class="mt-3 max-w-sm text-sm leading-relaxed text-muted-foreground">
        This help article is being written. In the meantime, you can browse the help
        index or ask our assistant directly in chat.
      </p>

      <div class="mt-8 flex flex-col items-center gap-3 sm:flex-row">
        <a
          href="/help"
          class="inline-flex items-center gap-2 rounded-md border border-border bg-card px-4 py-2 text-sm text-foreground transition-colors hover:bg-muted focus:outline-none focus:ring-2 focus:ring-[hsl(var(--color-primary-bright))] focus:ring-offset-2 focus:ring-offset-background"
        >
          ← Back to help
        </a>
        <a
          href="/chat"
          class="inline-flex items-center gap-2 rounded-md bg-[hsl(var(--color-primary-bright))] px-4 py-2 text-sm font-medium text-background transition-colors hover:bg-[hsl(var(--color-primary-bright)/0.9)] focus:outline-none focus:ring-2 focus:ring-[hsl(var(--color-primary-bright))] focus:ring-offset-2 focus:ring-offset-background"
        >
          Ask in chat →
        </a>
      </div>
    </div>

  </div>
</Layout>
