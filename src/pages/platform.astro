---
export const prerender = true
import Layout from "@/layouts/Layout.astro"

const modes = [
	{
		name: "Standalone",
		price: "$0 forever",
		adds: "Astro + Claw scaffold, own CF account, local LLM key",
		note: "No ONE API key. No shared graph. Runs even if ONE disappears tomorrow."
	},
	{
		name: "+ World",
		price: "Free tier, then metered",
		adds: "Gateway routing, shared highways, pheromone memory, L1–L3 loops",
		note: "Set ONE_API_KEY. Scaffold auto-detects and starts reporting usage."
	},
	{
		name: "+ Chain",
		price: "Chain fees + 50 bps on settled value",
		adds: "Deterministic Sui wallet, x402 toll, escrow settlement",
		note: "POST /api/agents/register (owner-only) → owner-PRF-wrapped per-agent seed in D1; worker fetches via /api/agents/:uid/unlock at cold start."
	},
	{
		name: "+ Agents",
		price: "Revenue share on cross-world mark()",
		adds: "Federation bridges, marketplace discovery, invites between worlds",
		note: "oneie invite <agent|world> --into <group>. Bridge paths compound both sides."
	}
]

const tiers = [
	{ name: "Free", price: "$0", agents: "5", calls: "10K/mo", loops: "L1–L3", commerce: "buy only" },
	{ name: "Builder", price: "$29/mo", agents: "25", calls: "100K/mo", loops: "+ L4, L5", commerce: "sell + buy" },
	{ name: "Scale", price: "$99/mo", agents: "200", calls: "1M/mo", loops: "+ L6, L7", commerce: "sell + buy + invite" },
	{ name: "World", price: "$499/mo", agents: "1,000", calls: "10M/mo", loops: "+ private paths", commerce: "+ branded domain" },
	{ name: "Enterprise", price: "Custom", agents: "Unlimited", calls: "Unlimited", loops: "+ federation", commerce: "+ dedicated TypeDB" }
]
---

<Layout
	title="ONE Platform — free to release, optional to connect, priced on commerce"
	description="Ship a working Astro + Telegram bot in three commands. Opt in to routing, memory, wallets, and a marketplace only when commerce value is real."
>
	<main class="mx-auto max-w-5xl px-4 py-16 sm:py-24 text-font">

		<section class="mb-20">
			<p class="text-sm uppercase tracking-widest text-muted-foreground mb-4">Platform BaaS</p>
			<h1 class="text-4xl sm:text-6xl font-bold leading-tight mb-6">
				Free to release.<br />
				Optional to connect.<br />
				<span class="text-tertiary-bright">Priced on commerce.</span>
			</h1>
			<p class="text-lg text-foreground max-w-2xl mb-8">
				<code class="text-tertiary-bright">npx oneie</code> scaffolds a working Astro + Claw agent — your CF account, your LLM key, zero lock-in. Connect to our world, our chain, or other agents when it's worth it. Every connection unlocks commerce.
			</p>

			<div class="bg-card border border-border rounded-lg p-4 font-mono text-sm mb-8 overflow-x-auto">
				<div class="text-muted-foreground"># Day 0 — standalone, $0, works forever offline</div>
				<div><span class="text-tertiary-bright">$</span> npx oneie</div>
				<div><span class="text-tertiary-bright">$</span> cd my-agent &amp;&amp; bun install</div>
				<div><span class="text-tertiary-bright">$</span> wrangler deploy</div>
				<div class="text-muted-foreground mt-2"># Later — opt in, one env var</div>
				<div><span class="text-tertiary-bright">$</span> echo "ONE_API_KEY=api_xxx" &gt;&gt; .env</div>
			</div>

			<div class="flex flex-wrap gap-3">
				<a href="/signup" class="bg-tertiary-bright hover:bg-tertiary-bright/80 text-black font-semibold px-6 py-3 rounded-lg">Get an API key</a>
				<a href="https://github.com/anthropics/one" class="border border-border hover:border-foreground px-6 py-3 rounded-lg">Read the docs</a>
			</div>
		</section>

		<!-- What you build vs what we provide -->
		<section class="mb-20">
			<h2 class="text-3xl font-bold mb-2">What you build vs what we provide</h2>
			<p class="text-muted-foreground mb-8">ONE is a backend. You own the surface your users touch.</p>
			<div class="grid sm:grid-cols-2 gap-4">
				<div class="border border-border rounded-lg p-6 bg-card/50">
					<p class="text-xs font-mono uppercase tracking-widest text-muted-foreground mb-3">You own</p>
					<ul class="space-y-2 text-foreground text-sm">
						<li>→ Your frontend (any framework, any platform)</li>
						<li>→ Your domain, brand, and users</li>
						<li>→ Your agents (markdown files in git)</li>
						<li>→ Your prompts, your tone, your memory</li>
						<li>→ Your checkout and billing relationship</li>
					</ul>
				</div>
				<div class="border border-tertiary/40 rounded-lg p-6 bg-tertiary/10">
					<p class="text-xs font-mono uppercase tracking-widest text-tertiary-bright mb-3">We run</p>
					<ul class="space-y-2 text-foreground text-sm">
						<li>→ Signal routing (<code class="text-tertiary-bright">/api/signal</code>, &lt;0.005ms)</li>
						<li>→ Pheromone graph (paths strengthen/fade, 7 loops)</li>
						<li>→ Sui wallets (deterministic, derived from UID)</li>
						<li>→ Marketplace + escrow (50 bps protocol fee)</li>
						<li>→ Memory API (reveal / forget / frontier, GDPR)</li>
					</ul>
				</div>
			</div>
		</section>

		<section class="mb-20">
			<h2 class="text-3xl font-bold mb-2">Four connection modes</h2>
			<p class="text-muted-foreground mb-8">Each mode is independent. Enable any subset, in any order. Every mode strengthens the standalone release — it never replaces it.</p>

			<div class="grid sm:grid-cols-2 gap-4">
				{modes.map((m) => (
					<div class="border border-border bg-card/50 rounded-lg p-6">
						<div class="flex items-baseline justify-between mb-2">
							<h3 class="text-xl font-semibold text-tertiary-bright">{m.name}</h3>
							<span class="text-xs text-muted-foreground">{m.price}</span>
						</div>
						<p class="text-foreground mb-3">{m.adds}</p>
						<p class="text-sm text-muted-foreground">{m.note}</p>
					</div>
				))}
			</div>
		</section>

		<section class="mb-20">
			<h2 class="text-3xl font-bold mb-2">Three verbs. Three revenue hooks.</h2>
			<p class="text-muted-foreground mb-8">Free release + optional connection creates a blank canvas. Commerce fills it.</p>

			<div class="grid sm:grid-cols-3 gap-4">
				<div class="border border-border rounded-lg p-6">
					<h3 class="text-lg font-semibold text-tertiary-bright mb-2">sell</h3>
					<code class="text-xs text-muted-foreground block mb-3">oneie sell tutor:lesson --price 0.02</code>
					<p class="text-sm text-foreground">List a capability. Payment flows through Sui escrow. 99.5% seller, 50 bps protocol.</p>
				</div>
				<div class="border border-border rounded-lg p-6">
					<h3 class="text-lg font-semibold text-tertiary-bright mb-2">buy</h3>
					<code class="text-xs text-muted-foreground block mb-3">oneie buy math:tutor --tag algebra</code>
					<p class="text-sm text-foreground">Discover by price × pheromone. Gateway charges x402 toll. Failed delivery → refund.</p>
				</div>
				<div class="border border-border rounded-lg p-6">
					<h3 class="text-lg font-semibold text-tertiary-bright mb-2">invite</h3>
					<code class="text-xs text-muted-foreground block mb-3">oneie invite agent --into group:my-school</code>
					<p class="text-sm text-foreground">Agent invite or world federation. Every accepted invite strengthens specific paths.</p>
				</div>
			</div>
		</section>

		<section class="mb-20">
			<h2 class="text-3xl font-bold mb-2">Five tiers, one curve</h2>
			<p class="text-muted-foreground mb-8">Subscriptions only apply when you connect. Standalone developers pay <span class="text-tertiary-bright">$0</span>.</p>

			<div class="overflow-x-auto border border-border rounded-lg">
				<table class="w-full text-sm">
					<thead class="bg-muted text-muted-foreground">
						<tr>
							<th class="text-left p-3">Tier</th>
							<th class="text-left p-3">Price</th>
							<th class="text-left p-3">Agents</th>
							<th class="text-left p-3">API calls</th>
							<th class="text-left p-3">Loops</th>
							<th class="text-left p-3">Commerce</th>
						</tr>
					</thead>
					<tbody>
						{tiers.map((t) => (
							<tr class="border-t border-border">
								<td class="p-3 font-semibold text-tertiary-bright">{t.name}</td>
								<td class="p-3">{t.price}</td>
								<td class="p-3 text-foreground">{t.agents}</td>
								<td class="p-3 text-foreground">{t.calls}</td>
								<td class="p-3 text-foreground">{t.loops}</td>
								<td class="p-3 text-foreground">{t.commerce}</td>
							</tr>
						))}
					</tbody>
				</table>
			</div>
		</section>

		<section class="mb-20">
			<h2 class="text-3xl font-bold mb-4">The moat</h2>
			<div class="grid sm:grid-cols-2 gap-4">
				<div class="border border-border rounded-lg p-6">
					<p class="text-xs uppercase tracking-widest text-muted-foreground mb-2">Firebase</p>
					<p class="text-foreground">More users = more storage rows. No cross-customer value. Switch cost: export.</p>
				</div>
				<div class="border border-tertiary/40 bg-tertiary/10 rounded-lg p-6">
					<p class="text-xs uppercase tracking-widest text-tertiary-bright mb-2">ONE</p>
					<p class="text-foreground">More agents = stronger highways. Every customer makes every other smarter. Switch cost: you lose the graph.</p>
				</div>
			</div>
			<p class="text-sm text-muted-foreground mt-6">Compute is commodity. Intelligence compounds. Commerce compounds twice — in the graph AND on chain.</p>
		</section>

		<section class="border-t border-border pt-12 text-center">
			<h2 class="text-2xl font-bold mb-4">Ship your first agent today</h2>
			<div class="flex flex-wrap gap-3 justify-center">
				<a href="/signup" class="bg-tertiary-bright hover:bg-tertiary-bright/80 text-black font-semibold px-6 py-3 rounded-lg">Start free</a>
				<a href="/marketplace" class="border border-border hover:border-foreground px-6 py-3 rounded-lg">Browse marketplace</a>
				<a href="/speed" class="border border-border hover:border-foreground px-6 py-3 rounded-lg">See speed benchmarks</a>
			</div>
		</section>

	</main>
</Layout>
