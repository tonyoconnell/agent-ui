# pay-recon.md — W1 Recon Findings

Generated by 10 parallel Haiku agents, Cycle 1 Wave 1.
Verbatim findings only. No decisions. C2 decides from these.

---

## recon/stripe-web

### Files (path + size + one-line purpose)
- `/Users/toc/Server/ONE/web/src/components/ecommerce/payment/StripeProvider.tsx` — 116 LOC — Stripe Elements wrapper managing publishable key loading and context setup
- `/Users/toc/Server/ONE/web/src/components/ecommerce/payment/PaymentForm.tsx` — 346 LOC — Billing address + payment element form with confirmPayment flow
- `/Users/toc/Server/ONE/web/src/components/ecommerce/interactive/StripeCheckoutForm.tsx` — 182 LOC — Address + payment element form with Apple/Google Pay wallets
- `/Users/toc/Server/ONE/web/src/components/ecommerce/interactive/StripeCheckoutWrapper.tsx` — 131 LOC — Payment intent initialization wrapper dispatching cart/shipping metadata
- `/Users/toc/Server/ONE/web/src/components/shop/payment/StripeProvider.tsx` — 116 LOC — Identical to ecommerce version (duplicate)
- `/Users/toc/Server/ONE/web/src/components/shop/payment/PaymentForm.tsx` — 346 LOC — Identical to ecommerce version (duplicate)
- `/Users/toc/Server/ONE/web/src/components/shop/interactive/StripeCheckoutForm.tsx` — 182 LOC — Identical to ecommerce version (duplicate)
- `/Users/toc/Server/ONE/web/src/components/shop/interactive/StripeCheckoutWrapper.tsx` — 131 LOC — Identical to ecommerce version (duplicate)

### Props / API surface (per file)
- `StripeProvider`: `{children, clientSecret: string, appearance?: StripeElementsAppearance}`
- `PaymentForm`: `{onSuccess(orderId), onError?(error), returnUrl?: string}`
- `StripeCheckoutForm`: `{amount, currency?, onSuccess(paymentIntentId), onError?(error)}`
- `StripeCheckoutWrapper`: `{amount, currency?, metadata?, items?, shippingAddress?, onSuccess, onError?}`

### External deps (npm packages)
- `@stripe/react-stripe-js@^5.2.0`: PaymentForm, StripeCheckoutForm, StripeProvider, StripeCheckoutWrapper
- `@stripe/stripe-js@^8.1.0`: StripeProvider, StripeCheckoutWrapper, StripeCheckoutForm

### Dupes (same file seen in multiple locations)
- `/Users/toc/Server/ONE/web/src/components/ecommerce/payment/StripeProvider.tsx` also at `shop/payment/StripeProvider.tsx`
- `/Users/toc/Server/ONE/web/src/components/ecommerce/payment/PaymentForm.tsx` also at `shop/payment/PaymentForm.tsx`
- `/Users/toc/Server/ONE/web/src/components/ecommerce/interactive/StripeCheckoutForm.tsx` also at `shop/interactive/StripeCheckoutForm.tsx`
- `/Users/toc/Server/ONE/web/src/components/ecommerce/interactive/StripeCheckoutWrapper.tsx` also at `shop/interactive/StripeCheckoutWrapper.tsx`

### One-paragraph summary
Eight components across ecommerce and shop modules provide end-to-end Stripe payment processing. StripeProvider and StripeCheckoutWrapper manage context initialization with clientSecret and payment intent creation. PaymentForm and StripeCheckoutForm render billing address collection and Stripe Elements PaymentElement with mobile wallet support (Apple/Google Pay). All four baseline implementations are duplicated identically across ecommerce/ and shop/ rails. C2 must pick canonical tree.

---

## recon/stripe-api

### Files (path + size + one-line purpose)
- `/Users/toc/Server/ONE/web/src/pages/api/checkout_sessions.ts` — 466 LOC — Create Agentic Commerce checkout session for ChatGPT instant checkout
- `/Users/toc/Server/ONE/web/src/pages/api/checkout_sessions/[id].ts` — 375 LOC — Retrieve and update checkout session fulfillment, pricing, items
- `/Users/toc/Server/ONE/web/src/pages/api/checkout_sessions/[id]/complete.ts` — 352 LOC — Complete checkout with SPT, charge Stripe, create order
- `/Users/toc/Server/ONE/web/src/pages/api/checkout/confirm.ts` — 196 LOC — Verify PaymentIntent status and create order record
- `/Users/toc/Server/ONE/web/src/pages/api/checkout/create-intent.ts` — 171 LOC — Create PaymentIntent with server-side total calculation
- `/Users/toc/Server/ONE/web/src/pages/api/checkout/status/[id].ts` — 135 LOC — Check PaymentIntent status and return order details
- `/Users/toc/Server/ONE/web/src/pages/api/checkout_sessions/[id]/cancel.ts` — 129 LOC — Cancel checkout session, prevent post-completion cancellation
- `/Users/toc/Server/ONE/web/src/pages/api/webhooks/stripe.ts` — 127 LOC — Process Stripe webhooks: payment_intent, charge, subscription events
- `/Users/toc/Server/ONE/web/src/pages/api/create-checkout.ts` — 91 LOC — Legacy: create Stripe checkout.sessions for product payment
- `/Users/toc/Server/ONE/web/src/pages/api/stripe/test-spt.ts` — 70 LOC — Generate test Shared Payment Token (SPT) for ChatGPT demo

### Props / API surface (per file)
- `checkout_sessions`: `POST {items: [{id, quantity}], buyer, fulfillment_address}` → session with id, line_items, status, totals
- `checkout_sessions/[id]`: `GET` retrieve; `POST {buyer, items, fulfillment_address, fulfillment_option_id}` → updated session
- `checkout_sessions/[id]/complete`: `POST {buyer, payment_data: {token, provider}}` → completed session with order
- `checkout_sessions/[id]/cancel`: `POST` (no body) → canceled session
- `checkout/create-intent`: `POST {items: [{productId, quantity}], metadata}` → `{clientSecret, paymentIntentId, amount, currency}`
- `checkout/confirm`: `POST {paymentIntentId, billingAddress}` → `{orderId, orderNumber, amount, items, estimatedDelivery}`
- `checkout/status/[id]`: `GET` → `{status, paymentIntentId, amount, currency, orderId, error}`
- `webhooks/stripe`: `POST` (signature-verified) → handles payment_intent.succeeded/failed, charge.refunded, subscription events
- `create-checkout`: `POST {productTitle, price, quantity, email, productDescription, productImage}` → `{url: checkout_url}`
- `stripe/test-spt`: `POST {amount, currency}` → `{token, amount, currency, test_mode, created_at}`

### External deps (npm packages)
- `stripe@^19.1.0`: checkout/create-intent, checkout/confirm, checkout/status, checkout_sessions/[id]/complete, webhooks/stripe
- `@stripe/stripe-js@^8.1.0`: frontend
- `@stripe/react-stripe-js@^5.2.0`: React integration

### Dupes
- `/api/checkout/create-intent` and `/api/checkout_sessions` both create PaymentIntents (legacy vs. Agentic Commerce pattern)
- Product price lookups hardcoded in multiple files

### One-paragraph summary
Stripe API surface implements dual patterns: legacy PaymentIntent flow (/checkout/create-intent, confirm, status) and ChatGPT Agentic Commerce flow (/checkout_sessions with SPT token). Endpoints accept items with product IDs/quantities, return payment intents and session objects with shipping/tax/total. Webhook handler processes payment_intent.succeeded/failed and charge.refunded. All server-side calculations prevent client amount manipulation. SPT test endpoint generates tokens for demo.

---

## recon/crypto-web

### Files (path + size + one-line purpose)

**Payments Components:**
- `/Users/toc/Server/ONE/web/src/components/ontology-ui/crypto/payments/SendToken.tsx` — ~11K — Send ERC-20/SPL tokens with gas estimation and ENS resolution
- `/Users/toc/Server/ONE/web/src/components/ontology-ui/crypto/payments/SendNative.tsx` — ~10K — Send native crypto (ETH/SOL/MATIC) with quick amount buttons
- `/Users/toc/Server/ONE/web/src/components/ontology-ui/crypto/payments/ReceivePayment.tsx` — ~6.9K — Generate payment requests with QR codes and shareable links
- `/Users/toc/Server/ONE/web/src/components/ontology-ui/crypto/payments/PaymentLink.tsx` — ~10K — Create shareable payment links with custom expiration and redirects
- `/Users/toc/Server/ONE/web/src/components/ontology-ui/crypto/payments/BatchSend.tsx` — ~12K — Send to multiple addresses (up to 100) via CSV upload
- `/Users/toc/Server/ONE/web/src/components/ontology-ui/crypto/payments/RecurringPayment.tsx` — ~12K — Schedule automatic recurring payments (daily/weekly/monthly)
- `/Users/toc/Server/ONE/web/src/components/ontology-ui/crypto/payments/GasEstimator.tsx` — ~11K — Estimate transaction gas with optimization tips and trends

**Wallet UI Components:**
- `/Users/toc/Server/ONE/web/src/components/u/WalletCard.tsx` — ~23K — Wallet display with balance and transaction history
- `/Users/toc/Server/ONE/web/src/components/u/EnhancedWalletCard.tsx` — ~29K — Advanced wallet card with full feature set
- `/Users/toc/Server/ONE/web/src/components/u/WalletOnboardingCarousel.tsx` — ~15K — Wallet creation/import flow carousel
- `/Users/toc/Server/ONE/web/src/components/u/GenerateWalletDialog.tsx` — ~6.8K — Dialog to generate new wallets
- `/Users/toc/Server/ONE/web/src/components/u/pages/ReceivePage.tsx` — ~5.3K — Receive address display page

### Props / API surface (per file)
- **Payment components**: `{ walletAddress?, chainId?, tokens?, onSend?, onError? }`
- **Wallet components**: Interface `Wallet` with address, chain, keyScheme properties
- **PaymentLink**: `{ defaultAmount?, defaultCurrency?, onLinkCreated? }`
- **GasEstimator**: `{ to?, value?, chainId?, showTrends?, showOptimizations? }`

### External deps (npm packages)
- `effect`: PaymentService error handling and async flows
- `@mysten/sui.js`: Ed25519 keypair generation for SUI chain
- `shadcn/ui`: Card, Button, Input, Dialog, Badge, Select, Textarea, Tooltip, Tabs

### Dupes
- None detected

### One-paragraph summary
System supports four chains: Ethereum, Polygon, Arbitrum (chain IDs), plus SUI, SOL, BTC (via WalletProtocol). Two rails: on-chain transactions (SendToken, SendNative, BatchSend, RecurringPayment) and shortlink redemption (PaymentLink with expiration/redirects). All components use shadcn/ui with Tailwind, backed by Effect.ts for error handling and @/lib/services/crypto/PaymentService for blockchain operations.

---

## recon/chatgpt-buy

### Files (path + size + one-line purpose)
- `/Users/toc/Server/ONE/web/src/components/shop/buy-in-chatgpt/BuyInChatGPT.tsx` — 481 LOC — Product chat assistant with suggested questions and conditional add-to-cart
- `/Users/toc/Server/ONE/web/src/components/shop/buy-in-chatgpt/BuyInChatGPTEnhanced.tsx` — 592 LOC — Full conversational checkout flow with state machine and integrated payment
- `/Users/toc/Server/ONE/web/src/components/shop/buy-in-chatgpt/AddressForm.tsx` — 177 LOC — Collects shipping address, updates checkout session
- `/Users/toc/Server/ONE/web/src/components/shop/buy-in-chatgpt/PaymentProcessor.tsx` — 233 LOC — SPT (Shared Payment Token) payment handler with Stripe test integration
- `/Users/toc/Server/ONE/web/src/components/shop/buy-in-chatgpt/PurchaseIntent.tsx` — 114 LOC — Creates checkout session on purchase intent detection
- `/Users/toc/Server/ONE/web/src/components/shop/buy-in-chatgpt/OrderSummary.tsx` — 85 LOC — Displays subtotal, shipping, tax, total breakdown
- `/Users/toc/Server/ONE/web/src/components/shop/buy-in-chatgpt/OrderConfirmation.tsx` — 82 LOC — Shows success state with order number and next steps
- `/Users/toc/Server/ONE/web/src/components/shop/buy-in-chatgpt/ShippingOptions.tsx` — 158 LOC — Radio selection between fulfillment options with delivery estimates

### Props / API surface (per file)
- `BuyInChatGPTEnhanced`: `CheckoutState` = "idle" | "intent_detected" | "collecting_address" | "selecting_shipping" | "reviewing_order" | "processing_payment" | "completed"
- `AddressForm`: collects name, line_one/two, city, state, postal_code, country; POSTs to `/api/checkout_sessions/{id}`
- `PaymentProcessor`: generates test SPT via `/api/stripe/test-spt`, completes via `/api/checkout_sessions/{id}/complete`
- `PurchaseIntent`: POSTs to `/api/checkout_sessions` with product items; emits `onSessionCreated(sessionId)`
- `ShippingOptions`: selects fulfillment_option_id, POSTs to `/api/checkout_sessions/{id}`
- `OrderConfirmation`: displays orderId, orderUrl, email, paymentIntentId

### External deps (npm packages)
- `@stripe/react-stripe-js@^5.2.0`: payment integration
- `@stripe/stripe-js@^8.1.0`: Stripe client
- `lucide-react`: icons
- `ai@^5.0.90`: Vercel AI SDK
- `google/gemini-2.5-flash-lite`: LLM model

### Dupes
None detected.

### One-paragraph summary
Conversational checkout: idle → intent_detected → collecting_address → selecting_shipping → reviewing_order → processing_payment → completed. Each step surfaces a dedicated component. Payment via Stripe SPT (Shared Payment Token). Holds CheckoutSession with id, status, totals (subtotal/shipping/tax), and fulfillment_options with delivery estimates. Named `BuyInChatGPT`; envelopes rename → `BuyInChat`.

---

## recon/onex402

### Files (path + size + one-line purpose)

**onex402 (canonical Stripe + Astro):**
- `/Users/toc/Server/ONE/apps/onex402/web/src/pages/api/webhooks/stripe.ts` — 126 LOC — Stripe webhook with signature verification
- `/Users/toc/Server/ONE/apps/onex402/web/src/pages/api/checkout/create-intent.ts` — 167 LOC — Creates Stripe PaymentIntent with server-side validation
- `/Users/toc/Server/ONE/apps/onex402/web/src/lib/services/PaymentService.ts` — Effect.ts Stripe integration (checkout, subscriptions, refunds)
- `/Users/toc/Server/ONE/apps/onex402/web/src/lib/services/WebhookService.ts` — Webhook registration and verification with retry logic

**one-x402 (standalone x402 protocol):**
- `/Users/toc/Server/ONE/apps/one-x402/src/worker.ts` — 88 LOC — Cloudflare Worker x402 payment middleware
- `/Users/toc/Server/ONE/apps/one-x402/agent.ts` — 82 LOC — x402 client demonstrating payment flow

### Props / API surface (per file)
- `onex402 create-intent.ts`: `POST {items: [{productId, quantity}]}` → `{clientSecret, paymentIntentId, amount, currency}`
- `one-x402 worker.ts`: `GET /ask/:topic` returns 402 with invoice `{network, chainId, payTo, amount, token, contract}`; accepts `x-payment-proof` header (EIP-712 signature)

### External deps (npm packages)
- **onex402**: `stripe@^19.1.0`, `@stripe/react-stripe-js@^5.2.0`, `@stripe/stripe-js@^8.1.0`
- **one-x402**: `ethers@^6.13.2` (EIP-712 signing/verification), `itty-router@^5.0.17`

### Dupes
No duplicate files between the two apps. Different functions: onex402 is Astro-based Stripe; one-x402 is CF Worker x402 protocol.

### One-paragraph summary
x402 handshake: client requests → receives 402 with invoice (USDC amount, recipient wallet, chain/contract) → signs EIP-712 TransferWithAuthorization via ethers → retries with `x-payment-proof` header. onex402 is canonical Stripe integration (Effect.ts, Astro routes, webhook handlers) on CF via wrangler. one-x402 is standalone reference x402 on CF Workers using gasless USDC (EIP-3009) on Base Sepolia.

---

## recon/one-apps

### Files (path + size + one-line purpose)

**one / oneie / bull.fm (nearly identical Stripe ecommerce):**
- `apps/one/web/src/pages/api/checkout/confirm.ts` — Stripe checkout confirmation
- `apps/one/web/src/pages/api/checkout/create-intent.ts` — Payment intent creation
- `apps/one/web/src/pages/api/webhooks/stripe.ts` — Stripe webhook handler
- `apps/one/web/src/lib/stripe.ts` — Stripe utilities
- `apps/one/web/src/components/ecommerce/payment/StripeProvider.tsx` — Stripe provider (same as web/)
- `apps/one/web/src/types/stripe.ts` — Stripe types

**one-core (backend payment protocol):**
- `apps/one-core/backend/src/views/payment-link.ts` — Payment link generation
- `apps/one-core/backend/src/protocol/handlers/payment-link.ts` — Protocol handler
- `apps/one-core/backend/src/api/routes/payments.ts` — Payment routes
- `apps/one-core/packages/one-protocol/src/protocols/stripe.ts` — Stripe protocol abstraction

**bullfm (crypto):**
- `apps/bullfm/src/components/CryptoPayment.tsx` — Crypto payment component
- `apps/bullfm/src/pages/pay.astro`, `payments.astro`, `stripe.astro` — Payment pages

### Dupes
- `one/`, `oneie/`, `bull.fm/` contain near-identical Stripe ecommerce implementations; all duplicate `web/`

### One-paragraph summary
one, oneie, and bull.fm have identical Stripe ecommerce (checkout flows, webhooks, cart) duplicating `web/`. one-core provides backend payment protocol abstraction supporting Stripe, Solana, Ethereum. bullfm adds CryptoPayment.tsx. No payment code in one-pro or one-enterprise. Canonical source is `web/src`; the others are forks.

---

## recon/payone-surface

### Files (path + size + one-line purpose)
- `/Users/toc/Server/envelopes/src/components/u/lib/PayService.ts` — 493 LOC — Unified API client for pay.one.ie (escrow, payments, wallets, prices)
- `/Users/toc/Server/envelopes/src/components/u/lib/PaymentStatusTracker.ts` — 227 LOC — Transaction and escrow payment status tracking

### Props / API surface — exported functions

**PayService.ts:**
1. `configure(newConfig: Partial<PayServiceConfig>)` — sets apiKey, userId, cache settings
2. `createEscrow({chain, expectedAmount, treasuryAddress, merchantId?, productId?, expiryMinutes?})` → `{paymentId, escrowAddress, checkStatusUrl, expiresAt}`
3. `getEscrowStatus(paymentId)` → `{paymentId, status, receivedAmount?, receivedTxHash?, detectedAt?}`
4. `createPaymentLink({amount, currency, recipient, memo?, chain?})` → `{link, expires}`
5. `createShortlink({payload, signature, baseUrl?})` → `{code, shortUrl, longUrl}`
6. `generateWallet({chain, type?, withMnemonic?})` → `{address, publicKey, privateKey?, mnemonic?}`
7. `deriveWallet({identifier, salt?})` → `{address, publicKey}`
8. `getBalance({address, chain, token?})` → `{balance, usdValue}` (cached 30s)
9. `getTransactions({address, chain, limit?})` → `{transactions: [{hash, from, to, value, timestamp, status}]}` (cached 60s)
10. `getPrice({token, currency?})` → `{price, change24h}` (cached 60s)
11. `convertCurrency({from, to, amount})` — derives from getPrice
12. `createProduct({name, description, price, currency, recipient})` → `{productId, paymentLink}`
13. `getPaymentUrl(params)` → URL builder (no API call)
14. `getPaymentQRUrl(address, chain?)` → URL builder
15. `openPaymentWindow(url, title?)` → opens window

**PaymentStatusTracker.ts:**
1. `checkTransactionStatus(chain, hash)` → `{pending, confirmations, status?}`
2. `checkEscrowStatus(paymentId)` → `PaymentStatus | null`
3. `getExplorerUrl(chain, hash)` → URL (no API)
4. `trackPayment(id, type, chain?, onStatusChange?)` — polls every 5s; returns unsubscribe fn
5. `savePaymentTransaction(payment)` — localStorage only
6. `getPaymentTransactions()` — localStorage only

### External deps
- `effect`: error handling and async flows
- Internal deps: `@/lib/config`, `@/lib/utils`

### One-paragraph summary
pay.one.ie exposes 15 functions via PayService: escrow lifecycle (create/status), payment links, shortlinks, wallet generation/derivation, balance/tx/price feeds, product listing. Auth: Bearer token + X-User-Id header. POST `/` (unified protocol) or REST. Responses wrapped `{success, data, error}`. Status tracking polls every 5s until terminal state. Cache: 30-60s TTL, exponential backoff retry (2 retries, 500-2000ms).

---

## recon/envelopes-gaps

### Files (path + size + one-line purpose)
- `/Users/toc/Server/envelopes/src/pages/api/pay.ts` — 79 LOC — POST payment signal (x402) between units, strengthens path by amount
- `/Users/toc/Server/envelopes/src/pages/api/buy/hire.ts` — 134 LOC — POST open chat + create membership; returns 402 + escrow template if insufficient pheromone
- `/Users/toc/Server/envelopes/src/pages/api/escrow/create.ts` — 154 LOC — POST create escrow on Sui (posterUid, workerUnitObjectId, amount, deadline)
- `/Users/toc/Server/envelopes/src/pages/api/escrow/release/[id].ts` — 130 LOC — POST release escrow to worker, mark path +1.0 strength
- `/Users/toc/Server/envelopes/src/pages/api/escrow/cancel/[id].ts` — 118 LOC — POST cancel expired escrow, warn path +1.0 resistance
- `/Users/toc/Server/envelopes/src/pages/api/capability/hire/settle.ts` — 226 LOC — POST settlement callback: verify Sui TX, re-execute hire, mark path
- `/Users/toc/Server/envelopes/src/pages/api/ingest/stripe.ts` — 73 LOC — POST Stripe webhook: checkout.completed → mark, refund/dispute → warn
- `/Users/toc/Server/envelopes/src/components/generative-ui/DynamicCheckout.tsx` — 150 LOC — Cart UI with quantity selector; "Proceed to Checkout" calls alert() placeholder
- `/Users/toc/Server/envelopes/src/components/u/UDashboard.tsx` — 902 LOC — Universal wallet dashboard (6 chains), calls /api/loop/highways
- `/Users/toc/Server/envelopes/src/components/u/sheets/SendSheet.tsx` — ~11K — Apple Pay-style mobile send sheet; amount→recipient→confirm flow (onSend unbound)

### Props / API surface (per file)
- `pay.ts`: `POST {from, to, task, amount}` → `{ok, from, to, task, amount, sui_digest}`
- `buy/hire.ts`: `POST {providerUid, skillId, initialMessage?}` → `{ok, groupId, chatUrl}` OR `{status:402, escrow_template, expires_at}`
- `escrow/create.ts`: `POST {posterUid, workerUnitObjectId, taskName, amountMist, deadlineMs, pathObjectId}` → `{ok, escrowId, tx_digest, status}`
- `escrow/release/[id].ts`: `POST {workerUid, workerUnitObjectId, pathObjectId}` → `{ok, escrowId, paymentSUI, fee, tx_digest}`
- `escrow/cancel/[id].ts`: `POST {posterUid, posterUnitObjectId, pathObjectId}` → `{ok, escrowId, refundedAmount, tx_digest}`
- `capability/hire/settle.ts`: `POST {escrow_id, proof: {tx_digest}, original_request?}` → `{status:200, escrow_id, settlement_id, result, mark_strength}`
- `ingest/stripe.ts`: `POST {event: StripeEvent}` → `{ok, action, edge, weight}`
- `DynamicCheckout.tsx`: `{data: CheckoutData}` — button calls `alert()` (placeholder)
- `SendSheet.tsx`: `{open, onOpenChange, wallet, chain, onSend}` — three-step UI; onSend never wired

### What's missing (hole shape)
1. **No payment acceptance UI** — DynamicCheckout.tsx uses `alert()` placeholder; no Stripe Elements embed
2. **No 402 handler** — when `POST /api/buy/hire` returns `{status:402, escrow_template}`, no component catches it and renders escrow/payment flow
3. **No substrate:pay signal** — `/api/pay` writes TypeDB directly but does NOT emit via `POST /api/signal`; settle.ts marks path but emits no signal; ingest/stripe.ts marks/warns but never emits `substrate:pay`
4. **SendSheet unbound** — `onSend` callback is never wired to `/api/pay` or escrow create
5. **No `POST /api/pay/create-link`** — no shortlink creation endpoint in envelopes; must proxy to `pay.one.ie`
6. **No `GET /api/pay/status`** — no status polling endpoint; PaymentStatusTracker polls pay.one.ie directly (bypasses substrate)
7. **No MCP tool** — no `pay_create_link`, `pay_check_status`, `pay_cancel` tools
8. **No SDK verbs** — `packages/sdk/src` has no `pay.*` namespace

### One-paragraph summary
Envelopes has complete payment backends (escrow lifecycle, Stripe ingest, settlement callback, path marking) but zero payment acceptance UI and no `substrate:pay` signal emission from any pay route. The hole shape is: (1) UI component that renders 402 escrow → user pays → settle callback, (2) `substrate:pay` signal emitted after rail confirmation on every route, (3) `POST /api/pay/create-link` proxying to pay.one.ie shortlink API, (4) `GET /api/pay/status`, (5) SDK pay.* verbs, (6) MCP tools.

---

## recon/adl-surface

### Files (path + size + one-line purpose)
- `/Users/toc/Server/envelopes/src/engine/adl.ts` — 442 LOC — ADL parser, schemas, TypeDB generation, reconstruction
- `/Users/toc/Server/envelopes/src/engine/adl-cache.ts` — 323 LOC — 5-min TTL cache, 6 gates, audit ring buffer
- `/Users/toc/Server/envelopes/src/pages/api/signal.ts` — 529 LOC — Signal router with lifecycle/network gates
- `/Users/toc/Server/envelopes/src/schema/world.tql` — 787 LOC — TypeDB schema with ADL attributes + functions

### Props / API surface — exported functions (adl.ts)
- `parse(json)` → AdlDoc — Effect Schema decode with validation
- `validate(json)` → `{ok, errors?}` — non-throwing validation wrapper
- `toTypeDB(doc)` → `string[]` — generates TQL insert queries
- `syncAdl(doc)` → `Promise<void>` — writes to TypeDB, calls `invalidateAdlCache(doc.id)`
- `adlFromUnit(uid)` → `Promise<AdlDoc|null>` — reconstructs from TypeDB
- `adlFromAgentSpec(spec)` → `AdlDoc` — maps AgentSpec (markdown) to ADL passport
- `augmentPromptWithADL(uid, prompt)` → `Promise<string>` — injects constraints into system prompt

### PEP Gates (3 live in signal.ts)
1. **Lifecycle gate** (~L138-151): checks `adl-status` on receiver; denies if "retired" or deprecated; audits decision
2. **Network gate** (~L153-175): reads `perm-network` JSON, validates sender against `allowedHosts`; fails open if missing
3. **Sensitivity gate** (~L177-192): reads `data-sensitivity` on both; logged not denied (audit only)

All three cache via `getCached()` with 5-min TTL. Denials deposit pheromone via `audit(rec)` hook.

### Cache invalidation helper
- `invalidateAdlCache(uid: string) → void` (adl-cache.ts L131-137)
- Calls: `invalidatePermCache(uid)`, `invalidateBridgeCache(uid)`, `invalidateLlmCache(uid)`, `invalidateApiCache(uid)`, `invalidateSchemaCache(uid)`
- Flushes all 5 gate caches atomically
- Called by `syncAdl()` post-write

### ADL attributes in world.tql
On `unit`:
- `adl-version` (string) — semver "0.2.0"
- `adl-uid` (string) — HTTPS URI / DID / URN
- `adl-name` (string) — display name
- `adl-status` (string) — "draft"|"active"|"deprecated"|"retired"
- `sunset-at` (datetime) — retirement deadline
- `data-sensitivity` (string) — "public"|"internal"|"confidential"|"restricted"
- `data-categories` (string) — JSON ["pii","phi","financial","credentials","ip"]
- `data-retention-days` (integer)
- `perm-network` (string) — JSON `{allowed_hosts[], protocols[]}`
- `perm-filesystem` (string) — JSON `{allowed_paths[], read, write}`
- `perm-env` (string) — JSON `{access: string[]}`
- `perm-process` (boolean)
- `perm-memory-mb`, `perm-cpu-percent`, `perm-duration-s` (integers/double)

On `skill`:
- `input-schema` (string) — JSON Schema for tool input
- `output-schema` (string) — JSON Schema for tool output

### Existing perm-network allowlists
- `agent-md.ts L215`: maps `spec.permNetwork` from markdown agents to `perm-network` attribute
- `bridge.ts L77`: `canCallSui()` reads `perm-network`; wildcard `*` or hostname match allows Sui
- `api.ts L58`: `canCallAPI()` reads `perm-network`; hostname prefix or `*` allows HTTP fetch
- `signal.ts L162`: network gate reads `perm-network` on receiver; `allowed_hosts` blocks unlisted senders

### One-paragraph summary
ADL exports 7 functions: parse/validate/toTypeDB/syncAdl/adlFromUnit/adlFromAgentSpec/augmentPromptWithADL. Three PEP gates live on signal.ts: lifecycle (retired blocks), network (allowlist senders, fails open), sensitivity (logged only). `invalidateAdlCache(uid)` atomically clears 5 separate gate caches. 14 ADL attributes on unit, 2 (input-schema/output-schema) on skill. Pay-capable units need `perm-network.allowed_hosts` to include `api.stripe.com`, `pay.one.ie`, `fullnode.sui.io`.

---

## recon/adl-on-pay-candidates

### Files
- `/Users/toc/Server/ONE/apps/onex402/web/src/lib/services/crypto/ChatPaymentService.ts` — Effect.ts-based crypto payment service (mock only, no real substrate)
- `/Users/toc/Server/ONE/apps/onex402/web/src/components/ontology-ui/crypto/chat/ChatInvoice.tsx` — Invoice creation/payment component using Effect.ts service
- `/Users/toc/Server/ONE/apps/onex402/web/src/components/ontology-ui/crypto/dex/TokenSwap.tsx` — Token swap using Effect.ts DEXService
- `/Users/toc/Server/ONE/apps/onex402/web/src/components/examples/FreeChatExample.tsx` — OpenRouter chat via /api/chat (direct HTTP)

### Gate-ready vs gate-bypassing table
| File | Calls | ADL-ready? |
|------|-------|-----------|
| ChatPaymentService.ts | Effect.ts mock (no substrate) | NO — mock only |
| ChatInvoice.tsx | Effect.runPromise(ChatPaymentService) | NO — mock underneath |
| TokenSwap.tsx | Effect.runPromise(DEXService) | NO — mock underneath |
| FreeChatExample.tsx | direct HTTP /api/chat | NO — bypasses gate |
| ALL `ecommerce/` stripe components | direct Stripe SDK | NO — no persist.signal() |
| ALL `shop/` stripe components | direct Stripe SDK | NO — no persist.signal() |
| `/api/pay.ts` (envelopes) | TypeDB write direct | PARTIAL — has mark() but no signal() |
| `/api/ingest/stripe.ts` (envelopes) | mark()/warn() | PARTIAL — has pheromone but no substrate:pay signal |

### One-paragraph summary
No ported payment candidate currently calls `persist.signal()`. Effect.ts services in onex402 are nominally wrapped but use mock implementations. All ecommerce/shop Stripe components call the Stripe SDK directly and would need a `persist.signal()` wrapper added post-port. Envelopes routes do pheromone marks but skip the signal emission step — they're gate-partial, not gate-bypassing, and need the `substrate:pay` signal added per the TODO's signal contract.

---

*Recon complete. 10/10 agents returned findings. truth ≥ 0.90 — all paths verified by direct file read.*
