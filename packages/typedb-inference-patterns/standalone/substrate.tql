# =============================================================================
# DEPRECATED — Use src/schema/one.tql instead
# =============================================================================
#
# The canonical ONE schema lives at:
#   /src/schema/one.tql
#
# This file was the old standalone version. It's kept for reference only.
# Do NOT load this file into TypeDB — it will conflict with one.tql.
#
# Differences from one.tql:
#   - Used "entity service" (removed — task with price IS a service)
#   - Used "pheromone-trail" (renamed to "trail")
#   - Used "payload" (renamed to "data")
#
# See: src/schema/one.tql for the converged schema
# See: packages/typedb-inference-patterns/standalone/seed.tql for bootstrap data
# =============================================================================
